#include "main.h"

/**
 * _islower - check whether it is lowercase or not
 * @c: the character to check
 *
 * Return: 1 if it is lowercase else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
